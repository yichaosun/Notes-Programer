```java
// 1,1,2 candidates target

class Solution{
  private List<List<Integer>> result = new ArrayList<List<Integer>>();
  
  public List<List<Integer>> getCombination(int [] candidates, int target){
    List<int[]> freq = getFrequency(candidates);
    List<Integer> combination = new ArrayList<Integer>();
		dsf(combination,freq,0,target);
    return result;
  }
  
  protected List<int[]> getFrequency(int [] candidates){
		List<int[]> freq = new ArrayList<int[]>();
    for(int num : candidates){
      int size = freq.size();
      if(0 == size || freq.get(size-1)[0]!=num){
        	freq.add(new int[]{num,1});
        continue;
      }
      ++freq.get(size-1)[1];
    }
    return freq;
  }
  
  protected void dsf(List<Integer> combination,List<int[]> freq,int pos, int target){
    if( 0 == target){
      	result.add(new ArrayList<Integer>(combination));
	      return;
    }
    if(pos == freq.size()){
      return;
    }
    
    dsf(combination,req,pos+1,target);
    
    // 选择pos
    if(){
      
    }
  }
  
}
```

